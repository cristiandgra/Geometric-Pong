import React from "react";
import ReactDOM from "react-dom";
import { GeometricPong } from "./GeometricPong";

ReactDOM.render(
  <React.StrictMode>
    <GeometricPong />
  </React.StrictMode>,
  document.getElementById("root")
);
